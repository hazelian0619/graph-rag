# Interaction Domain

Interaction 目录已按统一架构填充：

- `data/output/interaction/`：本地源数据快照（公开可用子集）
- `pipelines/`：interaction 相关构建与校验流水线（contracts/scripts/json reports）
- `products/interaction/current.json`：当前版本指针
- `release/external/interaction-l2-v1/`：对外发布包（结果性产物）

已执行 doc 对照自查：
- `interaction/release/external/interaction-l2-v1/manifests/interaction_doc_alignment_v1.json`
